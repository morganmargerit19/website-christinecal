{
  "name": "christinecal-v3",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "(tinacms build --skip-cloud-checks || echo 'Tina admin non construit (identifiants Tina Cloud absents) — le site est build sans /admin') && astro build",
    "build:site-only": "astro build",
    "build:strict": "tinacms build --skip-cloud-checks && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tinacms/astro": "^0.5.0",
    "astro": "^5.0.0",
    "gsap": "^3.12.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.5.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tinacms": "^3.9.3"
  }
}
